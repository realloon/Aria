{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pnpm: compile",
      "type": "shell",
      "command": "${env:HOME}/Library/pnpm/pnpm",
      "args": ["run", "compile"],
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
